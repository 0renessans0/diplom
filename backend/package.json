{
  "name": "factory-website-frontend",
  "version": "1.0.0",
  "description": "Frontend for Food Production Factory Simulator",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=8080",
    "dev": "npx live-server --port=8080 --watch=.",
    "serve": "python -m http.server 8080"
  },
  "dependencies": {},
  "devDependencies": {}
}